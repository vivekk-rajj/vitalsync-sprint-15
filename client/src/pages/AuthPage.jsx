import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { useAuth } from '../auth';
export default function AuthPage({ mode }) { const register = mode === 'register'; const { authenticate } = useAuth(); const nav = useNavigate(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const submit = async e => { e.preventDefault(); setError(''); try { await authenticate(register ? '/auth/register' : '/auth/login', form); nav('/dashboard'); } catch (x) { setError(x.message); } }; return <main className="auth-shell"><section className="auth-card"><span className="eyebrow">VITALSYNC CARE</span><h1>{register ? 'Create your account' : 'Welcome back'}</h1><p className="muted">Secure appointment coordination for better care.</p><form onSubmit={submit}>{register && <label>Name<input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /></label>}<label>Email<input required type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} /></label><label>Password<input required minLength="8" type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} /></label>{error && <p className="error">{error}</p>}<button>{register ? 'Create account' : 'Sign in'}</button></form><p className="muted">{register ? 'Already registered?' : 'New to VitalSync?'} <Link to={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Register'}</Link></p></section></main>; }
